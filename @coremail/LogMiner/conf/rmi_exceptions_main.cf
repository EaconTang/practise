;rmi exceptions classfied
;if multi grep needed,use value in format as example: option = ["grep1","gre2"]

[/All]
Exception = "WARNING"

[/All/Exception]
fail on = "Fail on"
msg e= "MessagingException"
rfof = "Reconnect fail on folder"
server fail = "Server fail"
skip msg = "Skip one message Exception(E)"
sync = "Sync mails failed"
